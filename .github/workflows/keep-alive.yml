name: Keep Render app awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -fsS -o /dev/null -w "HTTP %{http_code}\n" https://parcial-agile.onrender.com/
